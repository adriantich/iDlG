# iDlG
iDlG: individual Detection of linkage by Genotyping

## Install dependencies
```bash
conda create -f env.yaml
```
This will create an environment called IDLG. 

To set another name use the --name option

```bash
conda create --name env_name -f env.yaml
```
